# Vercel KV — أضيفي هذه المتغيرات في لوحة Vercel (Settings > Environment Variables)
# بعد إنشاء قاعدة KV من: Storage > Create Database > KV

KV_REST_API_URL=
KV_REST_API_TOKEN=

# اختياري — إذا استخدمت Upstash مباشرة:
# KV_URL=
# KV_REST_API_READ_ONLY_TOKEN=
